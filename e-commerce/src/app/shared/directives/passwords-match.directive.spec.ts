import { PasswordMatchDirective } from './passwords-match.directive';

describe('PasswordsMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordMatchDirective();
    expect(directive).toBeTruthy();
  });
});
